# tests/

`pytest`. Tests for Sentinel go in `tests/conduit_sentinel/` and use the expected values in `docs/SENTINEL_SPEC.md` section 11, read from `data/raw/organiser/`. Unit tests must not use the network.
