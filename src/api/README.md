# src/api/ (FastAPI)

Serves Sentinel and application outputs. First endpoints, from Sentinel: `GET /v1/stations`, `GET /v1/station-health`, `GET /v1/qc?days=`, `GET /v1/history?var=&start=&end=`. Application endpoints are added after decision 0003. OpenAPI docs at `/docs`.
